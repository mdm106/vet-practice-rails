<% content_for :title, "Update user details" %>

<% if current_user == @user %>
    <%= render 'form' %>
<% else %>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>
<% end %>
