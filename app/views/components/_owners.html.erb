<% if @owners.count > 0 %>
    <ul>
        The owners are:
        <% @owners.each do |owner| %>
        <li>
            <p><%= owner.full_name %></p>
        </li>
        <% end %>
        <%= will_paginate @owners %>
    </ul>
<% else %>
    <p>No owners found</p>
<% end %>