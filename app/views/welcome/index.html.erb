<% content_for :title, "Welcome" %>

<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<div class="container">
    <h1><%= @greeting %></h1>
</div>